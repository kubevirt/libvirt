# This is the default configuration: each of these variables can be
# overriden on the command line by using something like
#
#   $ IMAGE_NAME=my-libvirt ./hack/build.sh
#
# Note that in this case the variable must be specified *without*
# the CONF_ prefix.

CONF_IMAGE_NAME="kubevirt/libvirt"

CONF_FEDORA_VERSION="32"
CONF_LIBVIRT_VERSION="6.6.0-3"
CONF_QEMU_VERSION="5.1.0-6"

CONF_TARGET_ARCHITECTURES="amd64 arm64"
